 /* ExceptionHandler Class provides functionality to handle exceptions thrown during the execution of webserv.
 * It should log exception details using a Logger instance and be able to handle exceptions based on the specified log level.
 * It should be able to gracefully terminates webserv if necessary (e.getLogLevel() == CRITICAL). */