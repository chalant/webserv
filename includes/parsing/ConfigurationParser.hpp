#if !defined(CONFIGURATIONPARSER_HPP)
#define CONFIGURATIONPARSER_HPP

class ConfigurationParser
{
	private:
		/* data */
	public:
		ConfigurationParser(/* args */);
		~ConfigurationParser();
};

#endif
